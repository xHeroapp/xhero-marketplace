import Cart from "@/components/Cart";
import React from "react";

export const metadata = {
  title: "Cart",
};

const index = () => {
  return (
    <>
      <Cart />
    </>
  );
};

export default index;
